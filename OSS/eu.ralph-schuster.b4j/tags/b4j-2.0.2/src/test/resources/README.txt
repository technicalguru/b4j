The configurations stored here are suitable for systems directly accessing the internet.

In case you need to modify the configuration to apply special network setting, create a
file local-test-config.xml (Bugzilla tests) or local-test-jira-config.xml (Jira tests)
which contain your proxy settings.